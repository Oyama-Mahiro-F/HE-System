# 基于同态加密的隐私数据保护系统实现

## 课程项目报告

**所在学院**：北京邮电大学网络空间安全学院  
**所在班级**：_______________  
**学生学号**：_______________  
**学生姓名**：_______________  
**任课老师**：昌硕 老师  
**提交日期**：2026年6月

---

## 评分表

| 评分项 | 满分 | 得分 |
|--------|------|------|
| 项目选题与技术难度 | 15 | |
| 算法原理理解与阐述 | 20 | |
| 系统设计与实现 | 30 | |
| 实验验证与结果分析 | 20 | |
| 报告撰写质量 | 15 | |
| **总分** | **100** | |

| 姓名 | 学号 | 分工 | 贡献度 |
|------|------|------|--------|
| | | | |

---

## 摘要

随着大数据分析、联邦学习和隐私计算等技术的快速发展，如何在保护个体数据隐私的前提下实现有效的数据计算，已成为信息安全领域的前沿课题。同态加密（Homomorphic Encryption, HE）作为一种支持密文域直接运算的特殊密码体制，允许计算方在不接触明文的前提下对密文执行加法和/或乘法操作，为解决隐私保护与数据利用之间的矛盾提供了理论基础和实用工具。

本研究围绕同态加密技术展开系统性探索。在理论层面，深入剖析了RSA乘法同态方案的代数基础与安全性局限，并详细推导了Paillier加法同态密码体制的完整数学原理，包括密钥生成、加密随机化、解密算法及同态性质的形式化证明。在工程层面，采用Java语言实现了完整的同态加密系统，包含RSA乘法同态与Paillier加法同态双方案，并以"隐私保护工资统计"和"隐私保护电子投票"两个典型场景，展示了同态加密在实际隐私数据保护中的适用性。系统全部密码运算基于Java标准库（BigInteger + SecureRandom），零外部依赖。

实验验证涵盖算法正确性测试、同态性质验证、语义安全性分析及系统性能基准四个部分。结果表明：RSA方案能够实现精确的乘法同态运算，Paillier方案能够实现精确的加法同态运算及标量乘法，且Paillier方案具备语义安全性（相同明文每次产生统计独立的密文）。性能对比显示，RSA-2048在加密效率上具有显著优势（0.09ms vs Paillier-1024 4.94ms），而Paillier方案在密钥生成效率（18.16ms vs RSA 95.20ms）及解密效率（4.82ms vs RSA 10.13ms）方面表现更优，且Paillier加密过程的随机化特性使其天然具备语义安全性和IND-CPA安全性。

**关键词**：同态加密；RSA乘法同态；Paillier密码体制；隐私保护；安全计算；语义安全

---

## 第一章 绪论

### 1.1 研究背景及意义

#### 1.1.1 隐私计算时代的数据安全需求

随着《个人信息保护法》《数据安全法》等法律法规的相继实施，数据隐私保护已从技术选项升级为合规刚需。在医疗数据分析、金融风控建模、政府统计数据发布等场景中，数据使用方需要在保护个体隐私的前提下获取统计层面的信息。传统方法要求数据分析方先解密数据再进行计算，这意味着分析方必须持有解密密钥，存在数据泄露的根本性风险。

同态加密技术为这一困境提供了突破性的解决方案。其核心思想是：计算直接在加密数据上进行，得到的结果解密后与在明文上执行相同计算的结果一致。这样一来，数据分析方可以在不知道明文内容的情况下完成计算任务，从机制上消除了数据泄露的风险。

#### 1.1.2 同态加密的发展脉络

同态加密的发展可分为三个阶段：

1. **部分同态加密（PHE）时期（1978-2009）**：1978年，Rivest、Shamir和Adleman在提出RSA密码体制时，便注意到RSA天然满足乘法同态性质（E(m₁)·E(m₂) ≡ E(m₁·m₂) mod N），但当时他们将其视为RSA的安全隐患而非实用特性。1999年，Pascal Paillier提出了首个实用的加法同态密码体制（Paillier Cryptosystem），该方案基于合数模剩余类群的困难问题（Decisional Composite Residuosity Assumption, DCRA），支持任意次数的同态加法，且具备语义安全性。

2. **层次型全同态加密（L-FHE）时期（2009-2013）**：2009年，Craig Gentry在其博士论文中基于理想格构造了首个全同态加密方案，证明了同时支持加法和乘法同态在理论上的可行性。此后，BGV（2011）、BFV（2012）等第二代方案显著提升了效率。

3. **实用型全同态加密时期（2013至今）**：CKKS（2017）方案支持近似浮点数运算，使同态加密在机器学习推理等场景中开始走向实用化。微软SEAL、IBM HELib、Duality Technologies PALISADE等开源库的成熟，进一步降低了全同态加密的工程应用门槛。

尽管全同态加密在理论上已趋于成熟，但其计算开销仍比明文计算高出数个数量级。在多数实际应用场景中，部分同态加密（仅支持加法或仅支持乘法）已能满足需求。RSA乘法同态可应用于隐私保护乘法聚合，Paillier加法同态可广泛应用于电子投票、隐私保护统计等场景。本研究选取RSA乘法同态和Paillier加法同态两类经典部分同态加密方案，从理论推导、工程实现、应用场景和性能评估四个维度展开系统性研究。

#### 1.1.3 研究目的与内容

本研究的主要目的包括：（1）深入理解RSA和Paillier两种同态加密方案的数学原理和安全模型；（2）基于Java语言完成两种方案的工程实现，并进行严格的正确性验证；（3）设计并实现隐私保护工资统计和电子投票两个典型应用场景；（4）对比两种方案的同态性质、安全性和计算效率。

### 1.2 国内外研究现状

同态加密领域的学术研究主要沿两个方向推进：一是朝着"全同态"目标的理论突破，二是面向特定应用场景的"部分同态"优化。

**全同态加密方向**：自Gentry 2009年突破以来，Brakerski-Gentry-Vaithkunathan (BGV)、Brakerski-Fan-Vercauteren (BFV)等层次化方案将同态计算从理论推向工程。Cheon-Kim-Kim-Song (CKKS)方案针对实数运算的优化使其成为隐私保护机器学习的首选方案。2020年代以来，基于硬件加速（GPU/FPGA）的全同态加密实现不断涌现，推理延迟已从分钟级降至秒级。

**部分同态加密方向**：Paillier密码体制因其简洁的代数结构和良好的加法同态性质，在电子投票（Helios Voting System）、隐私保护数据聚合（Google RAPPOR的密码学升级版Prio）等领域得到广泛应用。ElGamal加密的乘法同态性质则被用于安全多方计算协议中的子模块。Boneh-Goh-Nissim（BGN）方案（2005）实现了"一次乘法+多次加法"的折中，在效率与功能之间达到更佳平衡。

**标准化进程**：ISO/IEC 18033-6已纳入Paillier加密标准。同态加密标准化联盟（HomomorphicEncryption.org）于2018年发布首版标准白皮书，持续推进安全性参数和API接口的规范化。

### 1.3 关键问题及技术挑战

1. **效率与功能的权衡**：全同态加密功能强大但效率低下，部分同态加密高效但功能受限。如何根据实际应用场景选择合适的方案是设计者面临的首要问题。

2. **安全性基础的理解**：RSA乘法同态的性质天然源于其代数结构（模幂运算），但这也意味着RSA用于同态加密时必须放弃填充机制（OAEP），从而丧失IND-CCA安全性。Paillier方案通过随机化因子r^N获得IND-CPA安全性，但其安全性依赖于DCRA假设。开发者需清晰理解每个方案的安全边界。

3. **密文域运算的语义正确性**：密文运算在模N（RSA）或模N²（Paillier）下进行，当明文运算结果超出模数范围时，同态解密结果将发生模溢出。设计应用协议时需确保明文的数值范围不超出模数约束。

---

## 第二章 同态加密算法原理

### 2.1 RSA乘法同态

#### 2.1.1 标准RSA回顾

RSA密码体系由Rivest、Shamir和Adleman于1978年提出，其安全性基于大整数因子分解的困难性。标准RSA方案包含三个算法：

**密钥生成** —— KeyGen(κ):
1. 随机选择两个大素数 p, q（各 κ/2 位）
2. 计算模数 N = p × q
3. 计算欧拉函数 φ(N) = (p-1)(q-1)
4. 选择公钥指数 e（通常 e = 65537），满足 gcd(e, φ(N)) = 1
5. 计算私钥指数 d = e^(-1) mod φ(N)
6. 输出公钥 pk = (N, e)，私钥 sk = (N, d)

**加密** —— Enc(pk, m)，其中 m ∈ Z_N:
```
c = m^e mod N
```

**解密** —— Dec(sk, c):
```
m = c^d mod N
```

解密的正确性由欧拉定理保证：c^d ≡ (m^e)^d ≡ m^(ed) ≡ m^(1+k·φ(N)) ≡ m · (m^φ(N))^k ≡ m (mod N)。

#### 2.1.2 RSA的乘法同态性质

RSA天然具备乘法同态性质，这是由其加密运算的代数结构（模幂运算）直接决定的。

设 m₁, m₂ ∈ Z_N，其密文分别为：
```
c₁ = E(m₁) = m₁^e mod N
c₂ = E(m₂) = m₂^e mod N
```

对两个密文进行模乘运算：
```
c₁ × c₂ mod N = (m₁^e mod N) × (m₂^e mod N) mod N
              = (m₁ × m₂)^e mod N
              = E(m₁ × m₂ mod N)
```

解密验证：
```
D(c₁ × c₂ mod N) = D((m₁ × m₂)^e mod N)
                 = ((m₁ × m₂)^e)^d mod N
                 = (m₁ × m₂)^(ed) mod N
                 = m₁ × m₂ mod N
```

通过归纳法推广到 n 个密文的情况：
```
D(∏ᵢ₌₁ⁿ cᵢ mod N) = ∏ᵢ₌₁ⁿ mᵢ mod N
```

**重要局限性**：
1. **仅支持乘法同态**：RSA的加密函数 f(x)=x^e mod N 对于模乘运算是同态的，但对于模加运算不同态：E(m₁+m₂) ≠ E(m₁) + E(m₂)，因为 (m₁+m₂)^e ≢ m₁^e + m₂^e (mod N)。
2. **无随机化**：标准RSA加密是确定性的（确定性函数），同一明文始终产生相同密文，因此不具备语义安全性（IND-CPA）。任何拥有公钥的攻击者都可以对候选明文逐一验证。
3. **密文可延展性**：攻击者可以在不知识明文的前提下构造新合法密文，例如 c' = c × E(k) = E(m × k)，在对选择密文攻击（CCA）模型中是不安全的。

#### 2.1.3 Textbook RSA的安全考虑

需要特别强调的是，本研究中RSA方案使用"textbook RSA"（无填充RSA），目的是保留其代数同态性质。在实际生产环境中，RSA加密必须使用OAEP（Optimal Asymmetric Encryption Padding）等填充机制以防止选择密文攻击。填充机制的加入会破坏乘法同态性质，因此RSA的同态加密应用与安全填充之间的张力是不可调和的。这是RSA乘法同态的根本性局限，也是Paillier等专门设计的同态加密方案在实用中的优势所在。

### 2.2 Paillier加法同态

#### 2.2.1 安全假设

Paillier密码体制的安全性基于**判定性合数模剩余假设**（Decisional Composite Residuosity Assumption, DCRA）：

给定合数 N = p × q（p, q为大素数）和整数 z ∈ Z*_{N²}，判定 z 是否为 N 次剩余（即是否存在 y ∈ Z*_{N²} 使得 z ≡ y^N mod N²）在计算上是不可行的。

等价地，区分随机元素和 N 次剩余在 Z*_{N²} 中是不可行的。DCRA假设的强度与RSA假设和大整数因子分解假设密切相关。

#### 2.2.2 密钥生成

**KeyGen(κ)**:
1. 随机选取两个大素数 p, q（各 κ/2 位，如 κ=1024 则各512位）
2. 计算模数 N = p × q
3. 计算 N² = N × N
4. 计算 Carmichael 函数 λ = lcm(p-1, q-1) = (p-1)(q-1) / gcd(p-1, q-1)
5. 选取生成元 g = N + 1（简化变体，满足 g ∈ Z*_{N²} 且 ord(g) 整除 Nλ）
6. 计算 μ = L(g^λ mod N²)^(-1) mod N，其中 L(x) = (x - 1) / N

由于 g = N + 1，利用二项式定理：
```
g^λ = (1 + N)^λ
    = 1 + λ·N + C(λ, 2)·N² + ...
    ≡ 1 + λ·N (mod N²)
```

因此 L(g^λ mod N²) = ((1 + λ·N) - 1) / N = λ，进而 μ = λ^(-1) mod N。

输出：
- 公钥 pk = (N, g, N²)
- 私钥 sk = (λ, μ)

#### 2.2.3 加密

**Enc(pk, m)**，其中 m ∈ Z_N：
1. 选取随机数 r ∈ Z*_N（即 gcd(r, N) = 1）
2. 计算 c = g^m × r^N mod N²

利用 g = N + 1 的简化，二项式展开：
```
g^m = (1 + N)^m
    = 1 + m·N + C(m, 2)·N² + ... + N^m
    ≡ 1 + m·N (mod N²)
```

因此加密公式简化为：
```
c = (1 + m·N) × r^N mod N²
```

**关键**：随机数 r 为每次加密引入独立随机性，确保相同明文产生不同密文，从而获得语义安全性（IND-CPA）。

#### 2.2.4 解密

**Dec(sk, c)**：
1. 计算 t = c^λ mod N²
2. 计算 m = L(t) × μ mod N

解密正确性推导如下：

```
c^λ mod N² = (g^m · r^N)^λ mod N²
          = g^(mλ) · r^(Nλ) mod N²
```

由卡迈克尔定理，对于任意 r ∈ Z*_{N²}，有 r^(λN) ≡ 1 (mod N²)，因此 r^(Nλ) ≡ 1 (mod N²)。

```
c^λ mod N² = g^(mλ) mod N²
          = (1 + N)^(mλ) mod N²
          ≡ 1 + mλ·N (mod N²)    (二项式展开，N² 项消失)
```

```
L(c^λ mod N²) = (1 + mλ·N - 1) / N = mλ
m = L(c^λ mod N²) × μ mod N = mλ × λ^(-1) mod N = m mod N
```

#### 2.2.5 同态加法性质

设 m₁, m₂ ∈ Z_N：
```
c₁ = E(m₁) = (1 + m₁·N) × r₁^N mod N²
c₂ = E(m₂) = (1 + m₂·N) × r₂^N mod N²
```

密文乘积（模 N²）：
```
c₁ × c₂ mod N² = [(1 + m₁·N)(1 + m₂·N)] × (r₁·r₂)^N mod N²
              = [1 + (m₁ + m₂)·N + m₁·m₂·N²] × (r₁·r₂)^N mod N²
              ≡ (1 + (m₁ + m₂)·N) × (r₁·r₂)^N (mod N²)    (N²项消失)
              = E(m₁ + m₂ mod N)                           (r₁·r₂ 仍为合法随机因子)
```

这表明密文域的模乘运算等同于明文域的模加运算。

#### 2.2.6 标量乘法性质

对密文进行模幂运算（指数为明文整数 k）：
```
c^k mod N² = [(1 + m·N) × r^N]^k mod N²
          = (1 + m·N)^k × (r^k)^N mod N²
          ≡ (1 + k·m·N) × (r^k)^N (mod N²)    (二项式展开)
          = E(k × m mod N)                     (r^k 仍为合法随机因子)
```

这表明 E(m)^k mod N² = E(k·m mod N)，即同态标量乘法（明文乘以任意常数）。

**注意**：标量 k 必须在 Z_N 中。同态标量乘法不能实现两个密文之间的同态乘法（那将构成全同态运算）。

#### 2.2.7 与RSA同态方案的形式化对比

| 维度 | RSA（Textbook） | Paillier |
|------|-----------------|----------|
| 同态运算类型 | 乘法同态 | 加法同态 + 标量乘法 |
| 运算模数 | N | N² |
| 加密随机性 | 无（确定性） | 有（r^N 因子） |
| 语义安全（IND-CPA） | 不满足 | 满足 |
| 安全假设 | 大整数因子分解 | DCRA（判定性合数模剩余） |
| 密文大小 | |c| ≈ |N| | |c| ≈ 2|N| |
| 加密计算量 | 1次模幂（mod N） | 1次模幂（mod N²）+ 1次模幂（mod N²）|
| 解密计算量 | 1次模幂（mod N） | 1次模幂（mod N²）|
| 同态运算类型 | 密文模乘 | 密文模乘 |
| 同态运算计算量 | 1次模乘（mod N） | 1次模乘（mod N²）|

### 2.3 应用场景的形式化建模

#### 2.3.1 隐私保护工资统计

设公司有 n 名员工，员工 i 的工资为 w_i ∈ Z_N。HR 持有 Paillier 公私钥对 (pk, sk)，公钥 pk 分发给所有员工。

**协议流程**：
1. 员工 i 加密其工资：c_i = Enc(pk, w_i)，将 c_i 上传至 HR
2. HR 在密文域计算总和：c_sum = ∏_{i=1}^n c_i mod N²
3. HR 解密密文总和：W = Dec(sk, c_sum)
4. HR 计算平均工资：W_avg = W / n（明文除法）

由 Paillier 加法同态性质保证：
```
W = Dec(sk, c_sum) = Σ_{i=1}^n w_i mod N
```

**安全保证**：HR 仅学习到总工资 W 和平均工资 W_avg，无法获取任何个体 w_i。在 Paillier 的 IND-CPA 安全性下，即使 HR 拥有公钥，也无法从密文 c_i 中区分 w_i = 15000 和 w_i = 30000。

#### 2.3.2 隐私保护电子投票

设 n 名投票人对某提案进行表决，投票 v_i ∈ {0, 1}（0=反对，1=赞成）。计票方持有 Paillier 公私钥对 (pk, sk)。

**协议流程**：
1. 投票人 i 加密其选票：c_i = Enc(pk, v_i)，提交密文
2. 计票方在密文域汇总：c_total = ∏_{i=1}^n c_i mod N²
3. 计票方解密：V = Dec(sk, c_total)
4. V 即为赞成票总数（0 ≤ V ≤ n），反对票 = n - V
5. 若 V > n/2，提案通过

由 Paillier 加法同态性质保证：
```
V = Σ_{i=1}^n v_i
```

**安全保证**：计票方仅学习赞成票总数，无法获知任何个体的投票选择。IND-CPA 安全性确保密文不泄露投票倾向。

---

## 第三章 系统设计与实现

### 3.1 系统架构

本系统采用分层模块化架构：

```
┌─────────────────────────────────────────┐
│              Main.java                   │
│   入口 + 正确性验证 + 性能基准测试       │
├─────────────────────────────────────────┤
│          PrivacyDemo.java                │
│   工资统计场景 + 电子投票场景            │
│   + RSA乘法同态演示                      │
├──────────────────┬──────────────────────┤
│ RSAHomomorphic   │   Paillier           │
│ RSA乘法同态      │   加法同态           │
│ (textbook RSA)   │   (Paillier'99)      │
├──────────────────┴──────────────────────┤
│           HEInterface.java               │
│           公共接口定义                    │
├─────────────────────────────────────────┤
│           KeyUtils.java                  │
│      BigInteger ↔ byte[] 序列化          │
├─────────────────────────────────────────┤
│       Java 8 JCA / BigInteger            │
│       (零外部依赖)                       │
└─────────────────────────────────────────┘
```

### 3.2 核心模块实现

#### 3.2.1 HEInterface 接口设计

```java
public interface HEInterface {
    void generateKeyPair(int keySize);
    BigInteger encrypt(BigInteger plaintext);
    BigInteger decrypt(BigInteger ciphertext);
    byte[] getPublicKeyEncoded();
    byte[] getPrivateKeyEncoded();
    String getAlgorithmName();
    BigInteger getOperationModulus();
}
```

接口定义了同态加密方案的标准操作集。通过接口抽象，系统可方便地扩展支持更多同态加密方案（如ElGamal乘法同态、BGN双线性同态等）。

#### 3.2.2 RSAHomomorphic 实现

**密钥生成**：
```
generateKeyPair(2048):
  primeSize = 1024
  p = BigInteger.probablePrime(1024, SecureRandom)
  q = BigInteger.probablePrime(1024, SecureRandom)
  N = p × q
  φ = (p-1)(q-1)
  e = 65537 (标准公钥指数)
  d = e^(-1) mod φ
```

**加密**：
```
encrypt(m):
  if m >= N: throw IllegalArgumentException (明文必须小于模数)
  return m^e mod N
```

**解密**：
```
decrypt(c):
  return c^d mod N
```

**同态乘法**：
```
homomorphicMultiply(c1, c2):
  return (c1 × c2) mod N
```

#### 3.2.3 Paillier 实现

**密钥生成**：
```
generateKeyPair(1024):
  p = BigInteger.probablePrime(512, SecureRandom)
  q = BigInteger.probablePrime(512, SecureRandom)
  N = p × q, N² = N × N
  λ = lcm(p-1, q-1) = (p-1)(q-1) / gcd(p-1, q-1)
  g = N + 1
  μ = λ^(-1) mod N  (简化，因为 L(g^λ mod N²) = λ)
```

**加密**：
```
encrypt(m):
  if m >= N or m < 0: throw IllegalArgumentException
  g_m = 1 + m × N (mod N²)    // 利用 g = N+1 的二项式展开简化
  r = random ∈ Z_N*
  r_n = r^N mod N²
  return (g_m × r_n) mod N²
```

**解密**：
```
decrypt(c):
  t = c^λ mod N²
  L = (t - 1) / N
  return L × μ mod N
```

**同态加法**：
```
homomorphicAdd(c1, c2):
  return (c1 × c2) mod N²
```

**同态标量乘法**：
```
homomorphicScalarMul(c, k):
  return c^k mod N²    // k 为 BigInteger 或 long
```

#### 3.2.4 KeyUtils 序列化工具

密钥采用 Length-Value（LV）编码格式，每个大整数以4字节长度前缀+字节数组表示：

```
编码格式: [len(4 bytes, big-endian)] [BigInteger.toByteArray()]
```

公钥/私钥由多个 LV 编码的 BigInteger 拼接而成。这种编码方式简洁、自描述，适合课程项目的演示需求。

### 3.3 应用场景实现

#### 3.3.1 隐私保护工资统计场景

采用 Paillier 加法同态方案：

1. **初始化**：HR 生成 Paillier-1024 密钥对，公钥(pk)分发给各部门员工
2. **加密上传**：5名员工分别加密自己的工资 w_i，将密文 c_i 发送给 HR
3. **密文求和**：HR 在密文域执行同态加法，计算 c_sum = ∏ c_i mod N²
4. **解密统计**：HR 用私钥解密 c_sum，得到总工资 W_total = 110000
5. **平均计算**：HR 在明文域计算平均工资 = W_total / n = 22000

**安全保证验证**：整个过程中 HR 仅接触密文 c_i（各 ~512 hex字符的随机值），无法从中推断个体工资。解密后仅得到聚合统计量。

#### 3.3.2 隐私保护电子投票场景

采用 Paillier 加法同态方案：

1. **初始化**：计票方生成 Paillier-1024 密钥对，公钥广播
2. **加密投票**：10名投票人各自加密选票 v_i ∈ {0, 1}，提交密文
3. **密文汇总**：计票方在密文域计算 c_total = ∏ c_i mod N²
4. **解密计票**：计票方解密 c_total 得赞成票总数 7，反对票 = 10 - 7 = 3
5. **结果判定**：7/10 > 1/2，提案通过

**安全保证验证**：计票方无法从密文判断任何个体的投票选择（0 或 1），投票隐私得到严格保护。

---

## 第四章 实验验证与性能分析

### 4.1 实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 Home China |
| JDK版本 | 1.8.0_91 (Oracle) |
| 加密提供者 | JCA默认 (SunRsaSign + built-in SecureRandom) |
| CPU | Intel Core (笔记本) |
| 外部依赖 | 无（零依赖） |

### 4.2 RSA乘法同态正确性验证

#### 测试1: 基础加解密

输入明文 m = 12345678901234567890，加密后解密，验证一致性。

**结果**：解密结果 = 12345678901234567890，与明文完全一致 ✓

#### 测试2: 同态乘法

m₁ = 777, m₂ = 999，验证 D(E(m₁) × E(m₂) mod N) = m₁ × m₂ mod N。

**计算过程**：
```
c₁ = 777^e mod N
c₂ = 999^e mod N
c_product = (c₁ × c₂) mod N
D(c_product) = 777 × 999 = 776223 ✓
```

#### 测试3: 乘法链

验证 D(E(2) × E(3) × E(5) × E(7)) = 210。

**计算过程**：
```
c = E(2) × E(3) × E(5) × E(7) mod N
D(c) = 2 × 3 × 5 × 7 = 210 ✓
```

### 4.3 Paillier加法同态正确性验证

#### 测试1: 基础加解密

输入 m = 100，加密后解密，验证一致性。

**结果**：解密结果 = 100 ✓

#### 测试2: 同态加法

m₁ = 50, m₂ = 30，验证 D(E(m₁) × E(m₂) mod N²) = m₁ + m₂。

**计算过程**：
```
c₁ = (1 + 50·N) × r₁^N mod N²
c₂ = (1 + 30·N) × r₂^N mod N²
c_sum = (c₁ × c₂) mod N²
     = (1 + 80·N) × (r₁·r₂)^N mod N²
D(c_sum) = 80 ✓
```

#### 测试3: 标量乘法

m = 25, k = 4，验证 D(E(m)^k mod N²) = k × m。

**计算过程**：
```
c = E(25) = (1 + 25·N) × r^N mod N²
c^4 mod N² = (1 + 25·N)^4 × (r^4)^N mod N²
          ≡ (1 + 100·N) × (r^4)^N (mod N²)
D(c^4) = 100 ✓
```

#### 测试4: 语义安全

对相同明文 m = 1 执行两次独立加密，比较密文。

**结果**：
- c₁ (hex): 3cf6d1a2...
- c₂ (hex): 13c73f13...
- 两密文相同：false ✓
- **验证**：Paillier加密的随机化因子 r^N 确保了语义安全性（IND-CPA）。

#### 测试5: 加法链

验证 D(Σ_{i=1}^{10} E(i)) = 55。

**结果**：解密 = 55 ✓

### 4.4 应用场景验证

#### 场景1: 工资统计

| 员工 | 工资 (元) | 密文长度 (hex chars) |
|------|-----------|---------------------|
| 1 | 15000 | 511 |
| 2 | 22000 | 511 |
| 3 | 18000 | 510 |
| 4 | 25000 | 512 |
| 5 | 30000 | 511 |
| **同态求和** | **110000** | **∑ 密文 = 总密文** |

验证：
- 总工资解密 = 110000，预期 = 15000 + 22000 + 18000 + 25000 + 30000 = 110000 ✓
- 平均工资 = 110000 / 5 = 22000 ✓
- HR 无法获取个体工资 ✓

#### 场景2: 电子投票

| 投票人 | 选择 | 密文 |
|--------|------|------|
| 1-10 | [1,1,0,1,0,1,1,0,1,1] | 各为独立密文 |

验证：
- 赞成票解密 = 7，预期 = 7 ✓
- 结果：7/10 > 1/2，提案通过 ✓
- 计票方无法获取个体投票 ✓

### 4.5 性能对比分析

性能测试采用5次预热（JIT编译）+ 20次迭代取平均值的方法，以减少JVM运行时优化的波动。

| 操作 | RSA-2048 (ms) | Paillier-1024 (ms) |
|------|---------------|---------------------|
| 密钥生成 | 95.20 | 18.16 |
| 加密 | 0.09 | 4.94 |
| 解密 | 10.13 | 4.82 |

**性能分析**：

1. **密钥生成**：Paillier-1024胜出（18.16ms vs RSA 95.20ms）。这是因为Paillier-1024的素数长度仅512位（两个512位素数），而RSA-2048的素数长度达1024位（两个1024位素数）。素数生成是密钥生成的主要开销（随机搜索+素性测试），O(k⁴)复杂度的米勒-拉宾测试使得1024位素数的生成成本远高于512位。

2. **加密**：RSA-2048显著胜出（0.09ms vs Paillier 4.94ms）。RSA加密仅需1次模幂运算 mod N（e=65537，可用17次模乘的快速幂），而Paillier加密需2次模幂 mod N²（分别计算 g^m 和 r^N），且模数 N² 的位宽约为2048位（比RSA模数大一倍），模乘运算量约为RSA的4倍。

3. **解密**：Paillier-1024胜出（4.82ms vs RSA 10.13ms）。Paillier解密仅需1次模幂 mod N²，RSA解密需1次模幂 mod N，但RSA的私钥指数d长度≈N（2048位），而Paillier的λ仅为N的一半（512位）。模幂运算复杂度与指数长度成正比，因此尽管Paillier的模数更大，较小的指数仍使其解密速度更快。

### 4.6 安全性分析

| 安全属性 | RSA (Textbook) | Paillier |
|----------|---------------|----------|
| 语义安全 (IND-CPA) | ✗ | ✓ |
| 选择密文安全 (IND-CCA) | ✗ | ✗ |
| 已知明文攻击抵抗 | ✗ (确定性加密) | ✓ |
| 安全假设 | 因子分解 | DCRA |
| 推荐密钥长度 | 2048+ | 1024+ (≈RSA-2048) |

**RSA textbook 不安全性说明**：由于不使用填充机制，攻击者可以对候选明文 m' 加密并比较 c' 是否等于 c，从而在已知明文集合中确定明文。这是确定型加密的固有弱点。在实际应用中，RSA的乘法同态性质通常仅在协议的特定子步骤中使用，而非直接暴露密文。

**Paillier IND-CPA安全性**：Paillier的IND-CPA安全性由随机因子 r^N 保证。在DCRA假设下，密文 c = (1+mN)·r^N mod N² 中的两部分 ((1+mN) 和 r^N) 在计算上不可区分，攻击者无法从密文中提取有关明文的任何部分信息。

---

## 第五章 总结与展望

### 5.1 工作总结

本研究围绕同态加密技术，完成了以下工作：

1. **理论层面**：系统研究了RSA乘法同态和Paillier加法同态两种部分同态加密方案的数学原理，推导了密钥生成、加密、解密、同态运算的完整代数过程，分析了各自的安全性基础和局限性。

2. **工程实现**：基于Java 8实现了完整的同态加密系统，包含HEInterface接口抽象、RSAHomomorphic乘法同态模块、Paillier加法同态模块、KeyUtils序列化工具，以及两个应用场景演示模块。系统零外部依赖，仅使用Java标准库。

3. **实验验证**：通过6项算法正确性测试（RSA 3项 + Paillier 5项 + 两部分重合计算）和2个应用场景演示，全面验证了同态性质的正确性和应用可行性。

4. **性能分析**：对两种方案在相同硬件环境下进行了20轮迭代的性能基准测试，分析了密钥生成、加密、解密三个核心操作的性能差异及其原因。

### 5.2 局限与展望

**本系统的局限性**：

1. RSA乘法同态使用textbook RSA，不具语义安全性，不可直接用于生产环境。
2. Paillier方案仅支持加法同态，无法处理复杂的统计分析（如方差、相关系数）。
3. 明文必须以BigInteger形式表示，缺乏对大文本或浮点数的直接支持。
4. 未实现安全信道（TLS），实际部署时需增加通信层安全保护。

**后续工作方向**：

1. **扩展同态方案**：实现BGN（Boneh-Goh-Nissim）方案，该方案利用双线性对实现"一次乘法+多次加法"同态，在功能性和效率之间取得更好的平衡。
2. **集成全同态加密库**：接入Microsoft SEAL或PALISADE，实现任意深度电路的密文计算，支持更复杂的隐私保护数据分析（如密文域机器学习推理）。
3. **浮点数支持**：采用CKKS方案的编码思想，将浮点数编码为整数多项式，支持密文域实数运算。
4. **安全多方计算融合**：将同态加密与安全多方计算（MPC）技术结合，构建更完善的隐私保护数据协作平台。

---

## 参考文献

[1] Rivest R L, Shamir A, Adleman L. A Method for Obtaining Digital Signatures and Public-Key Cryptosystems[J]. Communications of the ACM, 1978, 21(2): 120-126.

[2] Paillier P. Public-Key Cryptosystems Based on Composite Degree Residuosity Classes[C]. EUROCRYPT '99. Springer, 1999: 223-238.

[3] Gentry C. Fully Homomorphic Encryption Using Ideal Lattices[C]. STOC '09. ACM, 2009: 169-178.

[4] Brakerski Z, Gentry C, Vaikuntanathan V. (Leveled) Fully Homomorphic Encryption without Bootstrapping[C]. ITCS '12. ACM, 2012: 309-325.

[5] Fan J, Vercauteren F. Somewhat Practical Fully Homomorphic Encryption[J]. IACR Cryptology ePrint Archive, 2012: 144.

[6] Cheon J H, Kim A, Kim M, et al. Homomorphic Encryption for Arithmetic of Approximate Numbers[C]. ASIACRYPT '17. Springer, 2017: 409-437.

[7] Boneh D, Goh E J, Nissim K. Evaluating 2-DNF Formulas on Ciphertexts[C]. TCC '05. Springer, 2005: 325-341.

[8] ElGamal T. A Public Key Cryptosystem and a Signature Scheme Based on Discrete Logarithms[J]. IEEE Transactions on Information Theory, 1985, 31(4): 469-472.

[9] Katz J, Lindell Y. Introduction to Modern Cryptography (3rd Edition)[M]. CRC Press, 2020.

[10] Acar A, Aksu H, Uluagac A S, et al. A Survey on Homomorphic Encryption Schemes: Theory and Implementation[J]. ACM Computing Surveys, 2018, 51(4): 1-35.

[11] Albrecht M, Chase M, Chen H, et al. Homomorphic Encryption Standard[C]. Protecting Privacy through Homomorphic Encryption. Springer, 2021: 31-62.

[12] 杨波. 现代密码学（第5版）[M]. 清华大学出版社, 2022.
